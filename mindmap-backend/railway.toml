[build]
builder = "NIXPACKS"
watchPatterns = ["mindmap-backend/**"]

[deploy]
startCommand = "python server.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[deploy.healthcheckPath]]
path = "/health"

[[deploy.healthcheckTimeout]]
timeout = 300
